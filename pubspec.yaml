name: proje
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2  # SDK versiyonunuza göre uyarlayın

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2  # En son sürümü kontrol edip kullanın
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.8

  cupertino_icons: ^1.0.8

  # 1. DÜZELTME: Hataları gidermek için provider paketi eklendi.
  provider: ^6.1.2
  cloud_firestore: ^4.17.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Aşağıdaki "assets:" kısmı ve altındaki satırlar eklenmeli/düzenlenmelidir.
  assets:
    - assets/images/
    # İPUCU: Klasörün tamamını tanımlamak genellikle daha iyidir.
    # Ancak dilerseniz her dosyayı tek tek de belirtebilirsiniz:
    # - assets/images/logo.png
    # - assets/images/arkaplann.jpg